#!/usr/bin/env bash

# add bear env
export BEAR="`dirname $BASH_SOURCE`/../"

# add bear bin path
export PATH="${BEAR}/bin:${PATH}"

# add alias 
for file in `ls ${BEAR}/conf/alias`
do
    source ${BEAR}"/conf/alias/"$file
done


